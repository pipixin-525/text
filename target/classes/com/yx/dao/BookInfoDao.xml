<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yx.dao.BookInfoDao">
    <!--查询图书信息
    public List<BookInfo> selectBookInfo(@Param("pageSize") Integer pageSize, @Param("pageCurrent") Integer pageCurrent);-->
    <select id="selectBookInfo" resultType="com.yx.entities.BookInfo">
        select book_id, book_name, book_price, stock_id
        from db2.book_info
        limit #{pageCurrent},#{pageSize}
    </select>

    <!--查询数据笔数
    public Integer selectBookInfoCount();-->
    <select id="selectBookInfoCount" resultType="int">
        select count(*)
        from book_info
    </select>

    <!--查询图书单价
    public Double selectBookPriceByBookId(@Param("bookId") Integer bookId);-->
    <select id="selectBookPriceByBookId" resultType="double">
        select book_price
        from db2.book_info
        where book_id = #{bookId}
    </select>
</mapper>